import { createMuiTheme } from '../muIndex';

const FormTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#C02700',
    },
    secondary: {
      main: '#C02700',
    },
  },
});

export default FormTheme;
