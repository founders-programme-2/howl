import styled from 'styled-components';

const AttachImg = styled.img``;

const Input = styled.input``;

const Form = styled.form``;

const Label = styled.label``;

export { AttachImg, Input, Form, Label };
